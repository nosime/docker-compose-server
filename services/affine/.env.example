# AFFiNE service-specific configuration
# Global env provides: TZ, AFFINE_*, MAIL_*

# AFFiNE Database Configuration
POSTGRES_USER=affine
POSTGRES_DB=affine
DATABASE_URL=postgresql://affine:${AFFINE_POSTGRES_PASSWORD}@affine_postgres:5432/affine

# AFFiNE Application Configuration
AFFINE_SERVER_EXTERNAL_URL=${AFFINE_DOMAIN}
REDIS_SERVER_HOST=affine_redis
AFFINE_INDEXER_ENABLED=false
NODE_ENV=production

# Email Configuration - using global variables
MAILER_HOST=${MAIL_HOST}
MAILER_PORT=${MAIL_PORT}
MAILER_SECURE=${MAIL_SECURE}
MAILER_USER=${MAIL_USER}
MAILER_PASSWORD=${MAIL_PASS}
MAILER_SENDER=AFFiNE - ${MAIL_FROM_NAME} <${MAIL_FROM_ADDRESS}>

# Advanced AFFiNE settings (uncomment if needed)
# AFFINE_ADMIN_EMAIL=admin@your-domain.com
# AFFINE_ADMIN_PASSWORD=your_admin_password
# TELEMETRY_ENABLE=false
